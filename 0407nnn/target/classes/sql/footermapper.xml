<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace = "footer">
	<select id="cInfo" resultType = "hils.footer.controller.FooterDto">
		select * from company_info where info_num = 0
	</select>
	<update id="changeCInfo" parameterType="hils.footer.controller.FooterDto">
		update company_info set 
		company_name=#{company_name}, representative_name=#{representative_name}, 
		business_license_number=#{business_license_number}, 
		business_address=#{business_address}, phone_number=#{phone_number}, fax_number=#{fax_number} 
		where info_num=0
	</update>
</mapper>