<?xml version="1.0" encoding="UTF-8"?> 
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="manageUserMapper">

	<!-- 총 유저수 출력 -->
	<select id="UserCount" resultType="int">
		select count(*) from users_register
	</select>
	
	<!-- 페이징 처리 후 게시글 조회 -->
	<select id="UserSelect"
		resultType="hils.Register.Model.RegisterDto">
		select * from 
		(select ROWNUM RN, A.* from 
		(select * from users_register order by user_id) 
		A)
		where RN between #{start} and #{end}
	</select>	

	<!-- 회원 강제 탈퇴 -->
	<delete id="userDelete">
		delete from users_register where user_id = #{user_id}
	</delete>
	
	<!-- 회원 휴면 설정 -->
	<delete id="userDormancy">
		update users_register set enabled = 0 where user_id = #{user_id}
	</delete>

	<!-- 회원 휴면 해제 -->
	<delete id="userDormancyCancle">
		update users_register set enabled = 1 where user_id = #{user_id}
	</delete>
	
</mapper>