<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace = "usersetting">
	<!-- 설정 및 회원정보 mapper -->
	<select id="uInfo" resultType = "hils.Mypage1.controller.ProfileDto">
		select p.user_id user_id, translate(password,'1234567890','**********') password, 
		user_email, user_phone, is_hellinder_open, is_withboard, location 
		from users_profile p, users_register r  where p.user_id= 'jyjy'
	</select>
	<select id="getPw" parameterType="String" resultType = "String">
		select password from users_register where user_id=#{user_id}
	</select>
	<update id="changePw" parameterType="hils.Mypage1.controller.ProfileDto" >
		update users_register set password = #{password} where user_id=#{user_id}
	</update>
	<select id="getPhone" parameterType="String" resultType = "String">
		select user_phone from users_register where user_id=#{user_id}
	</select>
	<select id="phoneCheck" parameterType="String" resultType = "int">
		select count(*) from users_register where user_phone=#{user_phone}
	</select>
	<update id="changePhone" parameterType="hils.Mypage1.controller.ProfileDto">
		update users_register set user_phone=#{user_phone} where user_id=#{user_id}
	</update>
	<update id="changeEmail" parameterType="hils.Mypage1.controller.ProfileDto">
		update users_register set user_email=#{user_email} where user_id=#{user_id}
	</update>
	<update id="changeLoc" parameterType="hils.Mypage1.controller.ProfileDto">
		update users_profile set location=#{location} where user_id=#{user_id}
	</update>
	<update id="changeHilOpen" parameterType="hils.Mypage1.controller.ProfileDto">
		update users_profile set is_hellinder_open=#{is_hellinder_open} where user_id=#{user_id}
	</update>
	<update id="changeHilWB" parameterType="hils.Mypage1.controller.ProfileDto">
		update users_profile set is_withboard=#{is_withboard} where user_id=#{user_id}
	</update>
	
	
	<!-- 마이페이지 프로필 mapper -->
	<select id="profileInfo" parameterType="String" resultType = "hils.Mypage1.controller.ProfileDto">
		select user_id, weight, height, introduce, profile_img, is_hellinder_open
		from users_profile where user_id=#{user_id}
	</select>
	<update id="addImgFile" parameterType="hils.Mypage1.controller.ProfileDto">
		update users_profile set profile_img=#{profile_img} where user_id=#{user_id}
	</update>
	<select id="findProfileImg" parameterType="String" resultType = "String">
		select profile_img from users_profile where user_id=#{user_id}
	</select>
	
</mapper>

