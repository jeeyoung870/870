<?xml version="1.0" encoding="UTF-8"?> 
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="reportMapper">
								
	<!-- 신고글 작성 -->
	<insert id="writeReport">
		insert into report
		(r_number, reported_user, r_content, reporter_user, r_reg_date )
		values 
		(report_seq.NEXTVAL, #{reported_user}, #{r_content}, #{reporter_user},	sysdate)
	</insert>

	<!-- 신고글 목록 조회 -->
	<select id="selectReport" resultType="hils.Report.Model.ReportDto">
		select * from report order by r_number
	</select>
	
	<!-- 신고글 삭제 -->
	<delete id="deleteReport">
		delete from report where r_number = #{r_number}
	</delete>

</mapper>