<?xml version="1.0" encoding="UTF-8"?> 
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="registerMapper">
	
	<!-- 회원가입 -->
	<insert id="userRegister" parameterType="hils.Register.Model.RegisterDto">
		insert into user_regi
		(user_id, user_pw, user_name, user_phone, user_email)
		values 
		(#{user_id}, #{user_pw}, #{user_name}, #{user_phone}, #{user_email})
	</insert>

</mapper>