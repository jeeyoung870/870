<?xml version="1.0" encoding="UTF-8"?>
<beans:beans
	xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
           http://www.springframework.org/schema/security
           http://www.springframework.org/schema/security/spring-security.xsd">

	<http pattern="/css/**" security="none"/>
	<http pattern="/img/**" security="none"/>
	<http pattern="/images/**" security="none"/>
	<http pattern="/js/**" security="none"/>

	<http auto-config='true' use-expressions="true">
		<!-- 사용자 지정 로그인폼 페이지 요청주소 및 로그인 성공시, 실패시 요청 주소 지정 -->
		<form-login login-page="/user/loginform"
			
			default-target-url="/user/loginsuccess"
			login-processing-url="/user/j_spring_security_check"
			authentication-failure-url="/user/loginfail"
			username-parameter="user_id" 
			password-parameter="password" 
			always-use-default-target="true"/>
			
		<!-- 로그아웃 요청주소 지정 -->
		<logout logout-url="/hils/j_spring_security_logout" logout-success-url="/user/logout" />

		<intercept-url pattern="/hils/mypage" access="hasRole('ROLE_USER')" />
		<intercept-url pattern="/hils/usersetting" access="hasRole('ROLE_USER')" />
		<intercept-url pattern="/hils/timeline" access="hasRole('ROLE_USER')" />
		<intercept-url pattern="/**" access="permitAll" />

		<!-- <intercept-url pattern="/**" access="hasRole('ROLE_ADMIN')" /> -->

		<csrf disabled = "true" /> 
	</http>

	<authentication-manager>
		<authentication-provider>
			<jdbc-user-service data-source-ref="dataSource"
				users-by-username-query="SELECT user_id AS userName, password, enabled 
					FROM users_register WHERE user_id=?"
				authorities-by-username-query="SELECT user_id AS userName, auth AS authority 
					FROM users_register WHERE user_id=?" />
		</authentication-provider>
	</authentication-manager>
</beans:beans>